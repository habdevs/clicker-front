{

  "devDependencies": {
    "husky": "8.0.0",
    "lint-staged": "15.2.6"
  },
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "**/*.{scss,css}": [
      "stylelint --fix",
      "prettier --write"
    ]
  }
}
